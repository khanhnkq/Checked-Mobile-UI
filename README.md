# Checked-Mobile-UI

## Environment

Create a local environment file at the project root:

```env
API_BASE_URL=http://192.168.1.60:8080
```

You can also copy from `.env.example` and adjust the API URL for your machine/network.

